# Текущее состояние проекта

Дата продолжения: 2026-09-16. Ветка: `codex/domain-appointments-v2`.

## Откуда продолжать

Короткая точка восстановления: [continuation.md](continuation.md).

**Активная работа 16 сентября — программная разработка вне ручной репетиции.**
По прямому запросу владельца реализуется [срез 73: очередь писем](73-confirmation-email-outbox.md):
atomic enqueue, единый sender, claim/retry, stale-проверки, состояния в карточке.
Новая аддитивная миграция `0070` проверена на отдельной PostgreSQL; живой стенд
18000 остаётся на прежней версии. Реальная SMTP-доставка не включалась.
Окончательные результаты проверок и кодовый checkpoint будут записаны в срез 73.

**Текущий код и стенд — `3e0e9bcf1e85`, [срез 72](72-group-finance-clarity.md).**
Исправлена ясность программы/оплаты группы с сохранением исходных привязок.
14 тестов, независимое ревью и браузерные проверки пройдены; CI `34962449454`
успешен: 1265 passed, 532 warnings, 1063.84s, production backup/restore пройден.
102 таблицы совпали при переносе; web/db healthy, архив обновлён.
16 сентября в единственной открытой форме №2 снова индивидуальное 1+1,
программа/счёт пусты; прежнего черновика группы нет, причина не установлена.
Следующий шаг владельца — набрать группу 2+2 и сохранить. Агент только прочитал UI.
Ручная приёмка, другой ПК и хостинг остаются впереди.
Программные долги перечислены в конце среза72.

**История предыдущего исправления — `af36d612db76`, [срез 71](71-compact-people-picker-and-help.md).**
По уточнению владельца пояснения скрыты под «?», вместо полного списка людей
введён серверный поиск по 10 результатов и отдельный выбранный состав.
19 целевых тестов и браузер 320–1440 px на 1250 учебных людях пройдены,
ревью/Ruff/manifests приняты; полный CI `34877091703` успешен: 1265 тестов и backup/restore.
Владелец выбрал группу 2+2, но сохранение остановила проверка принадлежности
блока программы. Следующий шаг — очистить два необязательных поля программы/счёта
и сохранить учебную группу; неясность групповых настроек записана в журнал 67.
102 таблицы совпали при переносе, комплект healthy. Новый дизайн открыт
во второй вкладке; первую с несохранёнными пользовательскими правками не трогали.
Следующий шаг — оценка дизайна владельцем, затем рабочие задания по одному.

**История предыдущего исправления — `6349977c08d9`, [срез 70](70-schedule-and-appointment-form-clarity.md).**
После самостоятельного создания занятия №2 владелец сообщил о неясной форме
и нечитаемом календаре. Форма переработана, добавлен список занятий за день,
после создания открывается карточка с результатом. 60 целевых тестов,
независимое ревью, браузер 320–1440 px и проверки фильтров/ошибок пройдены.
CI `34866385869`: 1260 passed, 1 failed — старое ожидание select в prefill,
исправлено в 71. Все 102 таблицы перенесены с совпадением
содержимого, web/db healthy, архив другого ПК обновлён; `e693` остановлен.
Во вкладке владельца открыта форма редактирования занятия №2 на 17 сентября
10:00–10:45. Действий за него агент не сохранял. Следующий шаг — оценка
новой ясности интерфейса владельцем, затем дальнейшие задания по ролям.

**История предыдущего исправления — `e693f5ad478f`, [срез 69](69-specialist-request-visibility.md).**
Владелец нашёл итог больничного с подсказкой и попросил выделение заявки
и личные статусы в шапке. Доработка установлена с сохранением 102 таблиц;
21 целевой тест, независимое ревью и браузер 320/768/1024/1440 px пройдены.
CI `34859518787`: 1256 passed, 1 failed — устаревшее ожидание табличной разметки
после замены заявок карточками. Проверка исправлена в 70 и прошла локально.
Индикатор принят владельцем, создание занятия выполнено с замечаниями удобства.
Прежний `e9cc` остановлен, volumes сохранены.

**История предыдущего исправления — `e9cc9b243821`, [срез 68](68-time-off-decision-clarity.md).**
Во время согласования больничного владелец обнаружил неясную повторную форму.
Исправление установлено с сохранением содержимого 102 таблиц и текущего входа.
18 целевых тестов, Ruff, зависимости, независимое ревью и read-only браузерная
проверка пройдены. Общий CI `34854566904` успешен для точного SHA:
полный набор тестов и production backup/restore пройдены,
итог в `.runtime/time-off-ui-ci.log`. Комплект
`dist/RM-pilot-e9cc9b243821` healthy, прежний `e378` остановлен с сохранением
хранилищ. Владелец подтвердил решение как руководитель; следующий шаг —
проверить итоговый статус в кабинете специалиста.

**Текущая задача — [совместный тест 67](67-guided-role-acceptance.md).**
Владелец попросил сейчас проходить роли на текущем ПК по одному заданию
агента; вход специалистом и подача учебного больничного 14–16 сентября
подтверждены на открытой странице владельца. Администратор уже согласовал
заявку, руководитель подтвердил: в UI «Согласовать: Решение руководителя»,
очередь заявок пуста. Итог специалист увидел с подсказкой; новый индикатор принял.
После создания занятия приёмка продолжилась замечаниями формы/расписания в 70.
Нужно опробовать
другой ПК с Docker и внешний хостинг. Архив для другого ПК подготовлен
и проверен по manifest; доступа к другому ПК/площадке хостинга пока нет.
Эти новые условия не считаются закрытыми локальной приёмкой версии `e378d02`.

Дополнение 14 сентября: [протокол 65](65-field-rehearsal-protocol.md) теперь
содержит конкретные требования к ПК и людям, технический сеанс на 60 минут
с резервом до 2 часов и отдельный рабочий тест на 150 минут. Остаток работ
в разделе «Что остается» ниже актуализирован: принятые 61D-6/61D-7a–7c
не требуют повторной реализации. Код и комплект `e378d02` не менялись;
новый общий CI для этой правки документации не запускался.

### История checkpoint: защита истории участника, срез 66

14 сентября практическая репетиция выявила потерю ссылки финансового факта
при снятии нативного участника группы. Исправление
`e378d020b11f179056fcaf783a81123d6c8e6561` защищает форму и удаление в админке;
схема БД не менялась. 10 целевых тестов, статические проверки и независимое
ревью пройдены. [Общий CI 34801576103](https://github.com/Gordey-ap01/rm/actions/runs/34801576103)
успешен на точном SHA: **1253 passed, 524 warnings, 1112.71s (18m32s)**,
production backup/restore drill пройден. Лог `.runtime/participant-removal-ci.log`.

На момент среза 66 запущенный комплект — `dist/RM-pilot-e378d020b11f`.
В настоящем браузере проверено отклонение удаления участника после проведения
и списания; оба участия и ссылка единственной проводки сохранены.
БД теперь содержит 1 проведённую группу и 1 проводку, остатки 2 и 1.
Исходная копия `e378d020b11f-pretest-20260914.dump` (0 проводок) восстановлена
в отдельную PostgreSQL; данные совпали. Web/db healthy, только loopback-доступ.
Временные QA-контейнеры удалены. Старые комплекты остановлены с сохранением БД.

На предыдущем `5cae343` проверены создание, конфликт, перенос, ручные решения администратора и приоритет
руководителя, проведение, повторное списание, раздельные групповые списания,
прогресс, завершение каскада и программы. На `e378d02` выполнена описанная выше
проверка проведения, списания и отказа удаления. Точные версии, границы сценариев,
найденное мелкое дублирование текста конфликта и доказательства — в
[срезе 66](66-participant-history-preservation.md).
До полевого допуска остаются выбранный ПК, реальная перезагрузка
Windows и подтверждение участников/ответственного/времени. Приёмка людьми
и готовность рабочей площадки с реальными данными ещё не заявляются.

### История комплекта 64 и начальной репетиции 65 (предыдущая версия)

Дополнение 14 сентября — [полевой протокол 65](65-field-rehearsal-protocol.md).
На той же версии `5cae343` через настоящий браузер создано отдельное занятие
№2 на 14 сентября 10:00–10:30. Администратор принял расписание без запроса
специалисту, затем отметил проведение и посещение. Руководитель (группа
«Руководители», без is_staff) изменил факт на «Не проведено / Не пришел».
Обратная попытка администратора отклонена сообщением «Решение руководителя
может изменить только руководитель»; две записи истории проведения остались
с исходными авторами, ролями и основаниями. Попытка пересмотреть расписание
уже проведенного занятия ожидаемо отклонена: в репетиции расписание проверять
до отметки проведения. Консоль браузера без ошибок/предупреждений; сеанс
завершен и вкладка закрыта. Проверка не заменяет все сценарии рабочего дня.
Read-only подтверждение: `.runtime/pilot-field-ui-20260914.json`.
Теперь в текущей учебной БД 2 занятия, 0 проводок; у группы №1 на 21 сентября
по-прежнему 2 участника, статус proposed, ручных решений нет. Принятые ранее
копия и логи с 1 занятием фиксируют состояние до этой дополнительной репетиции.
Код приложения и принятый комплект не менялись, новый общий CI не требуется.

Финальная поставка находится в `dist/RM-pilot-5cae343ee720`, source commit —
`5cae343ee720b580f4a09ca4d1d65aa103d12463`. Это самостоятельный Docker Desktop
стенд без зависимости от окна Codex: `START_PILOT.bat` / `STOP_PILOT.bat`,
Windows PowerShell 5.1, URL `http://127.0.0.1:18000`, непубликованный PostgreSQL
и отдельные containers/volumes. Seed — 21 сентября 2026 года, четыре учетные
записи в трех ролях: администратор, руководитель и два специалиста. Пароль существует только как `RM_PILOT_PASSWORD` в
`pilot/.pilot-local.env`; значение не документировать и не логировать.

История поставки: `c45c97df8b6ae2d7da7d690e8b32eaabcd226569` создал
source-only изолированный комплект; `1c734b5dfa764a04402421f948faca08c92f7f77`
защитил committed bytes от Windows `core.autocrlf`;
`bfd3ea65f572222b4bf76398258e275fa7896b8d` перевел календарь и навигацию
руководителя на operator authority; `5cae343ee720b580f4a09ca4d1d65aa103d12463`
одной JS-правкой сохраняет закрытыми фильтры календаря при асинхронной загрузке
вариантов.

Локальные подтверждения: export 11 passed, 9.65s
(`.runtime/pilot-package-tests.log`); seed PostgreSQL 11 passed
(`.runtime/pilot-seed-postgresql.log`); settings 5 passed, 1.23s; director
schedule access 7 passed, 1 warning, 41.80s
(`.runtime/director-schedule-access.log`). На реальном Windows PowerShell 5.1
для `1c734b5`: Start, hash guard, concurrent Stop lock, отказ одноименного
проекта из другой папки и Stop/Start без image build; 4 users, 2 recipients,
1 appointment, 0 ledger, password unchanged. Orphan-volume guard `c45c97d` принят runtime,
disposable volume удален (`.runtime/pilot-kit-orphan-guard.log`).

[CI 34774667645](https://github.com/Gordey-ap01/rm/actions/runs/34774667645)
успешен на точном `bfd3ea6`: **1243 passed, 522 warnings, 1086.55s (18m06s)**;
restore passed (`.runtime/pilot-kit-ci.log`). Финальный
[CI 34778462107](https://github.com/Gordey-ap01/rm/actions/runs/34778462107)
успешен на точном `5cae343ee720b580f4a09ca4d1d65aa103d12463`:
**1243 passed, 522 warnings, 1045.22s (17m25s)**, backup/restore успешен;
полный лог `.runtime/pilot-kit-release-ci.log`.
Предыдущий общий [CI 34758296947](https://github.com/Gordey-ap01/rm/actions/runs/34758296947)
успешен: 1209 passed, 515 warnings, 1036.08s.

Фактический Start финального комплекта прошел с native exit 0
(`.runtime/pilot-kit-release-start.log`). В браузере проверены вход руководителя,
operator navigation и «Ставки»; `/schedule/?date=2026-09-21` показывает 1 group,
2 staff, 2 recipients, 10:00–10:45, пять API заполнены, console clean. Последняя
JS-правка принята в браузере: четыре listbox закрыты на load; поиск «Первый»
дает 1 staff/1 group, reset — 2 staff/1 group, меню закрыты. UI persistence
после Stop/Start принята: Stop и Start на Windows PowerShell 5.1 дали native
exit 0, хеш сгенерированного env не изменился, image build/pull не было,
повторный запуск не потребовал новых миграций; seed сохранил данные без дублей
(`.runtime/pilot-kit-release-stop.log`, `.runtime/pilot-kit-release-restart.log`).
Администратор сохранил учебную заметку и после Stop/Start прочитал ее в том же
UI-сеансе. Новый вход `pilot-specialist1` показывает только профиль
«Учебный Специалист Первый», без меню оператора, поиска и ссылки на общий календарь.

Read-only БД подтверждает users=4, recipients=2, appointments=1, ledger=0,
marker=1, note_persisted=true, `rm_pilot_training`, DEBUG=false и locmem mail
(`.runtime/pilot-kit-release-database.json`). Docker inspect: web/db healthy,
web опубликован только на `127.0.0.1:18000`, DB ports=0, user=rehab,
restart=unless-stopped (`.runtime/pilot-kit-release-verification.json`).
Техническая приемка 64 завершена; проект `5cae343` оставлен запущенным для
пользователя. Проверочный браузерный сеанс завершен, временная вкладка закрыта.
Ожидаются физический ноутбук выезда, настоящий Windows reboot и первый тест с людьми.

Предтестовая копия учебной БД: `dist/pilot-backups/5cae343ee720-pretest-20260914.dump`,
902104 байта, PostgreSQL custom format. SHA-256:
`187B01CB85C7E5BD084C814A4777F81D70BE4A689DD6BB2A0EE295BFC8D92E38`.
Восстановление в новую `rm_pilot_restore_qa` на PostgreSQL 17 прошло с exit 0:
4 пользователя, 2 получателя, 1 занятие, 0 проводок, 1 маркер seed; заметка
в `operations_child.notes` с датой `14.09.2026` сохранена. Временный контейнер
с tmpfs и без внешних портов удален, исходная БД не изменялась. Доказательства:
`.runtime/pilot-snapshot-qa-20260914.json` и одноименный `.log`; инструкция рядом
с dump. Это состояние после технической QA до теста сотрудников, а не нетронутый
seed. Копия содержит только БД и не заменяет резервирование файлов и настроек.

Пять существующих контейнеров пользователя не затронуты; старые кандидаты `1c`
и `bfd` остановлены с сохраненными volumes. После закрытия Codex контейнеры
финального предшественника оставались healthy около часа. Это не устанавливает
причину закрытия Codex и не доказывает ее исправление. Windows reboot и
физический ноутбук выезда не тестировались.

Следующий практический этап — проверить компьютер выезда и провести
репетицию первого выезда по [плану 63](63-first-field-test-and-pilot-plan.md): 21 сентября 10:00–12:30
при допуске 18 сентября. Реальный пилот — ориентир 5 октября при допуске
2 октября; запас 12 октября не заменяет закрытия условий.

Автоматический срез 63A (`7b6fb46`) соединяет рабочий день группы, операторские
решения, приоритет руководителя, прогресс, списания и закрытие каскадов.
`operations/tests/test_pilot_workday_acceptance.py`: **2 passed, 1 warning
за 41.34s на PostgreSQL**, native exit 0. Ruff и независимое ревью приняты.
Итоговая редакция проверена после добавления повтора CHARGE и audit assertions;
лог `.runtime/pilot-workday-postgresql.log`.
[Общий CI 34758296947](https://github.com/Gordey-ap01/rm/actions/runs/34758296947)
проверяет точный head `7b6fb46aeecf73c0890a5125780af7f8f9ccee97`.
Код приложения и схема в этом срезе не менялись. Учебная БД и локальная
репетиция позднее приняты в срезе 64 выше. Компьютер выезда, запуск после
перезагрузки Windows и тест с сотрудниками остаются открытыми.
Одноразовый `rm-pilot-workday-qa` с учебной PostgreSQL на 5448 после локальной
проверки удален; пять контейнеров пользователя продолжают работать.

Предыдущий принятый пакет с изменением приложения — 61D-7c (`34697fb`):
[CI 34754752273](https://github.com/Gordey-ap01/rm/actions/runs/34754752273)
успешен на `34697fbdce1fe7ab6d02ad8723662a687e45b1ba`: **1207 passed,
513 warnings за 1093.92s (18m13s)**; приложение 19m34s,
backup/restore 7m33s. Лог: `.runtime/program-overview-ci-success.log`.
Автоматическая общая приемка продолжена в 63A выше; затем ручной выезд и пилот.

Предыдущий пакет — 61D-7b (`a497013`):
[CI 34718948780](https://github.com/Gordey-ap01/rm/actions/runs/34718948780)
успешен на `a497013ec7e281aeeb1383bded9aa639e1fed817`: **1195 passed,
506 warnings за 801.31s**; приложение 14m27s, backup/restore 7m55s.
Лог: `.runtime/block-lifecycle-ci-success.log`.

61D-7c принят локально и в общем CI: существующие панель и очередь получили обзор программ
и каскадов, четыре фильтра, независимые страницы по 20 объектов, фактический
прогресс и переход к защищенным командам. Счетчики исключают повторы при
пересечении категорий; остановленные серии вынесены в отдельную ссылку.
GET не меняет историю/факты; роль проверяется до загрузки данных.
Общий PostgreSQL gate обзора и соседних dashboard/work_queue тестов —
**18 passed, 93 deselected, 1 warning за 47.93s**, native exit 0.
Browser QA выявил сохранение выбранного фильтра при fragment-only ссылке
«Всего в очереди»; исправлен путь, повтор UI gate — **7 passed за 41.06s**.
Логи: `.runtime/program-overview-final-postgresql.log`,
`.runtime/program-overview-filter-fix-postgresql.log`.
Независимое ревью принято; показаны 390px/1280px без переполнения,
фильтры и сброс проверены фактическим нажатием, переход к программе сохраняет
запрет возобновления администратором. Console чистая, viewport восстановлен,
вкладка закрыта. Ruff/manifests прошли. Общий CI 61D-7c также прошел.

Локальная диагностика подтвердила два падения основного процесса Codex:
12 сентября в 22:36:58 и 13 сентября в 07:36:50 (UTC+10). Сохранились только
метаданные Crashpad; дампов с исключением и стеком нет. Причина закрытия
приложения не установлена и не исправлена. Нельзя подменять этот факт
предположением о лимите модели, памяти, Docker или сети. Обезличенный отчет:
`.runtime/codex-crash-diagnostic-20260913.md`; в поддержку ничего не отправлялось.
При отправке `a497013` были TLS timeouts к GitHub; повторный push прошел.
Последующий read-only запрос gh с HTTP/2, отключенным только для этого процесса,
получил успешный CI. Причинную связь настройки с сетевым сбоем это не доказывает.
Код и миграции рабочего проекта сохраняются независимо от окна приложения.

После принятого обзора — сквозной сценарий рабочего дня и пилотная приемка;
новые доменные команды в этот срез не добавляются. Исторические доказательства:

61D-6 принят локально и в общем CI: активация/завершение/отмена программы с
рассмотренным составом, явными ролями и SQL guards. Код `67d61cd`;
[CI 34693080115](https://github.com/Gordey-ap01/rm/actions/runs/34693080115)
успешен на `67d61cdf0b140bde1799bb37de5869852dabe1e4`: **1154 passed,
498 warnings за 683.91s**. Job приложения — 12m30s, параллельное
восстановление резервной копии — 7m26s. Полный лог сохранен в
`.runtime/program-completion-ci-success.log`.

61D-7a принят локально и в общем CI: карточки программы/получателя показывают
план, назначения, фактически проведенные участия и остаток. Списания
сохраняются отдельно. Незакрытый каскад отображается по фактам как
«Запланирован», «Расписан» или «Идёт»; поздние факты не меняют явные
терминальные статусы. Мастер и три materializer-path используют условный
переход planned -> scheduled внутри транзакции назначения, без регресса
состояния или частичного сохранения. Общий локальный PostgreSQL gate
нового helper, проекции и lifecycle UI: **33 passed за 68.55s**, native exit 0;
лог `.runtime/program-progress-final.log`. Код сохранен в `5f538de`;
[общий CI 34696564220](https://github.com/Gordey-ap01/rm/actions/runs/34696564220)
на `5f538de82247afbda569c341cbc2700dc5479642` завершился: 1165 passed и
1 failed за 875.65s, 500 warnings. Единственный failure — UI-тест ожидал
старый текст пояснения после финальной правки шаблона. Ожидание исправлено;
тот же тест прошел отдельно (1 passed за 35.26s, native exit 0). Причина —
текст был изменен после локального gate; итоговая редакция должна проверяться
после последнего изменения. Повторный общий
[CI 34717334111](https://github.com/Gordey-ap01/rm/actions/runs/34717334111)
успешен на `2f83bcc548a93bf08c489e16bc84fef6e7d53eed`: **1166 passed,
500 warnings за 954.01s**; восстановление резервной копии также прошло.
Лог повторного CI — `.runtime/program-progress-copy-ci-success.log`. Лог первого
CI — `.runtime/program-progress-ci-failed.log`, исправления —
`.runtime/program-progress-copy-fix.log`. Restore job прошел.
Browser QA на синтетических данных: при фактических 390px показатели
каскада отображаются карточкой без горизонтальной прокрутки; при 1280px —
таблицей. Пример plan=4, allocated=2, completed=1, remaining=3, charged=2
совпадает в карточках программы и получателя. Обе страницы без page overflow,
console errors/warnings отсутствуют. Проверка выполнена на актуальных
исходных стилях (отдельный dev-server 8012 с DJANGO_DEBUG=1; старый QA 8011
отдавал ранее собранный staticfiles). Viewport восстановлен, вкладка закрыта.

Схема `0001-0068` развернулась в одноразовой PostgreSQL;
Django/Ruff/manifests/drift checks прошли. Независимое ревью выявило
необходимость проверки canonical fingerprint в SQL и инверсию порядка
блокировок в Django admin с inline-каскадами. Обе исправлены; реальная
конкурентная проверка admin POST подтверждает ожидание блока до захвата корня.

Новый модуль содержит 19 проверок. Последовательный PostgreSQL gate старых
миграционных проверок, нового модуля и измененной composition-фикстуры:
23 passed за 101.49s. UI-модуль: 21 passed за 32.60s. Отдельный старый
PostgreSQL concurrency-класс серий: 22 passed за 95.17s, native exit 0.
Первоначальный полный локальный файл серий оборвался без итогового отчета
после 116 отметок; причина завершения не установлена, успешным полным прогоном
он не считается. Повтор выполнен для конкурентного класса с faulthandler и
сохранением native exit; весь файл затем прошел в общем CI `34693080115`.

Ограниченная runtime-role выполнила activate/pause/resume/cancel, а UPDATE/DELETE
истории были запрещены. Browser QA: активация на 390px, досрочное завершение
руководителем, отмена, stale 409 и обновление проверки, terminal block form
без 500; названия услуг вместо внутренних ID, console чистая. Временные
QA-серверы 8008/8009 остановлены владельцем. Все данные синтетические.

Отдельно внесен `921b3a7`: CI разделен на независимые тесты приложения и
backup/restore drill. [Первый run 34692002951](https://github.com/Gordey-ap01/rm/actions/runs/34692002951)
успешен на `921b3a7`: 1132 passed, 495 warnings за 930.62s; job приложения
16m41s, отдельный restore job 7m30s. Общее ожидание около 17 минут. Это
измерение одного запуска; суммарная стоимость runner-минут не обязана снижаться.
Логи: `.runtime/ci-parallel-accepted.log`, `.runtime/program-completion-migrate.log`,
`.runtime/program-completion-series-regression.log`, `.runtime/program-completion-series-pg68.log`.

61D-7b принят локально и в общем CI: явное завершение/отмена каскада,
основание, роль автора, неизменяемая история и защита от повторной/старой формы.
Администратор завершает выполненный план; досрочное завершение доступно
руководителю. Отмена доступна обеим ролям. Рассмотренный снимок получается
одним SQL-запросом; обзорный трехзапросный helper не используется для решения.
Миграция `0069` защищает историю, статус и новые назначения, включая перенос
участника в другое занятие с сохранением ссылки на закрытый каскад.
Поздний учет существующего занятия, в том числе первая legacy-строка участия,
остается доступен. Закрытие каскада не переписывает расписание и деньги.

Итоговый локальный PostgreSQL gate пяти новых/смежных модулей: **36 passed,
1 warning за 75.52s**, native exit 0; `.runtime/block-lifecycle-final.log`.
Отдельно прошли миграционные и исправленные старые сценарии: **6 passed за
51.80s**; старый конкурентный admin inline сценарий вошел в UI gate **7 passed**.
Первый соседний регрессионный прогон дал 48 passed и 4 failed: исправлены
несовместимость формы с исключенным полем `program` и три старые фикстуры,
которые обходили новые запреты статуса/назначения. Ограниченная runtime-role
выполнила досрочное завершение и позднее проведение legacy-занятия;
UPDATE/DELETE истории запрещены (`.runtime/block-runtime-probe.log`).
Чистая схема `0001-0069`, пустой reverse/reapply, отказ reverse при истории,
Ruff, Django check, migration drift и manifests проверены. Финальная редакция
UI после уточнения последствий закрытия проверена на SQLite: **7 passed**.
Browser QA: руководитель досрочно завершил каскад (0 из 3), администратор
получил запрет досрочного завершения, отменил отдельный каскад и завершил
выполненный план (1 из 1). История содержит автора/роль/основание, кнопки
закрытого каскада недоступны. Проверены 390px и 1280px без page overflow;
console errors/warnings отсутствуют, viewport восстановлен. Общий CI нового
пакета прошел (точный head и результат в начале документа). Итоговое независимое ревью
приняло PostgreSQL-сценарий без блокирующих замечаний; предел гарантии прямого
сохранения события на SQLite отмечен в контракте (production использует PG).

После принятого 61D-7b в общем CI принят руководительский обзор 61D-7c;
общая ролевая и пилотная приемка остаются отдельным этапом.
Принятие пакета — контрольная точка, а не автоматическая остановка продолжения.

Docker 12 сентября запущен пользователем; API отвечает. Причина отсутствия
автозапуска — `AutoStart: false` в настройках Docker Desktop и отключенная
запись Docker Desktop в Windows StartupApproved (03). Настройки не менялись.
Рабочие БД не мигрировались. Временный контейнер для приемки
61D-6/61D-7a: `rm-program-lifecycle-qa`, порт 5448, только синтетические данные.
После локальной приемки QA-серверы остановлены, временный контейнер удален.
Пять контейнеров пользователя продолжают работать.

Приемка 13 сентября использовала отдельный `rm-block-lifecycle-qa` на порту 5448
и синтетические БД `rm_block_*`. Browser QA выполнялся на DEBUG-сервере 8013.
После приемки сервер завершен, одноразовый контейнер удален. Пять контейнеров
пользователя продолжают работать; рабочие БД не мигрировались.

Предыдущий полностью принятый функциональный пакет (историческая точка):

61D-5 принят локально и в общем CI: пауза/возобновление программы, журнал решений,
защита ручного назначения и новых определений групповых серий. Модель,
сервис, UI и миграция `0067` проверены.
Актуальный commit после исправления изоляции тестов —
`07b632f83a4281d3ab6a51a6854743258b041082`.
[Повторный CI 34317851480](https://github.com/Gordey-ap01/rm/actions/runs/34317851480)
завершился успешно на этом commit: `1132 passed, 495 warnings` за `725.86s`.
Весь job — 20 минут 31 секунда; backup/restore drill — 7 минут 6 секунд.
Функциональный код внесен в `dde62b6`; `07b632f` исправляет изоляцию тестов.
После обрыва сначала проверить текущий commit и результат CI;
не повторять уже пройденные локальные проверки без новой ошибки/изменения.

Активный этап — завершение рабочих сценариев программ, каскадов и серий
по контракту 61. Принятый до этого срез 61D-4 сохранен ниже;
полную готовность проекта к рабочему пилоту эти срезы не означают.

Границы переходов закреплены в разделах 61D-5/61D-6/61D-7b
контракта 61. После приемки переходов каскада остаются руководительский
обзор и общая ролевая приемка. Нельзя
превращать обычное редактирование статуса в массовую отмену расписания
либо финансовую корректировку.

Объяснение для владельца центра и порядок доведения до пилота:
[карта продолжения проекта](project-roadmap-2026-09-09.md).
Она является планом, а не заменой требований PRD.

## Принятый результат: 61D-5

- Пауза/возобновление программы имеют отдельные команды и неизменяемый журнал.
  Пауза доступна администратору/руководителю; возобновление — руководителю.
  Принятый UUID распознается до stale/status-проверок; история руководителя
  имеет приоритет. Старые paused-программы не получают вымышленный backfill.
- Пауза не переписывает расписание и финансовые факты. Новые назначения
  проверяют программу под блокировкой; обычные заметки и учет проведенного
  занятия остаются доступны. Возобновление само не создает занятия.
- Исправлена конкурентная перезапись привязки участника старой формой:
  сохранение считывает актуального участника под блокировкой, проверяет
  текущий и выбранный каскады; при отсутствии изменений запись не выполняется.
- Новые блокировки выявили race повторного UUID при создании/join серии:
  повтор мог завершиться ValidationError на full_clean до INSERT. Теперь
  обработчик проверяет уже принятый UUID и его исходные данные; при отсутствии
  такого ключа исходная ошибка остается ошибкой. Оба прежних PG-теста
  одновременного создания прошли после исправления; повторное ревью прошло.
- Чистая PostgreSQL-цепочка `0001-0067` прошла. Restricted runtime-role
  выполнила обе команды, а UPDATE/DELETE нового журнала были запрещены.
- Независимое ревью после исправления замечания прошло. Browser QA:
  admin pause, director resume, приоритет, stale 409, replay без нового события,
  история; Chrome 390px без переполнения и console errors. Обратная ссылка
  «К получателю» проверена; новый переход из карточки получателя отдельно
  в браузере не проверялся. Все данные синтетические.
- Новый модуль: PostgreSQL 18 passed за 22.546s; SQLite 15 passed и 3
  PostgreSQL-only skips. Включены raw SQL/deferred guards, migration round-trip,
  preflight и запрет reverse после истории; гонка подтверждает ожидание
  писателя через `pg_blocking_pids`, затем отказ назначения после паузы.
- Старые серии/реестр/состав/stop-resume: 192 passed, 4 failed в первом прогоне
  за 432.40s. Две проверки UUID исправлены в коде, две старые проверки
  адаптированы к новому запрету пустой серии и перечитыванию состояния.
  Все четыре прошли отдельно: UUID 2 passed за 36.42s; остальные 2 passed
  за 2.114s. Ruff, Django check, migration dry-run, manifests и diff-check чистые.
- Первый [общий CI 34315830339](https://github.com/Gordey-ap01/rm/actions/runs/34315830339)
  дал 1129 passed и 3 failed: старый класс операторских миграционных тестов
  оставлял схему на `0065` перед новыми PG-проверками. Добавлен tearDown,
  восстанавливающий текущие leaf nodes после assertions. Последовательный
  pytest старого класса и новых PG-тестов прошел: 11 passed за 27.33s.
  Повторный общий CI выше закрыл все 1132 теста; guards приложения не ослаблялись.
- Логи этого среза:
  `.runtime/program-lifecycle-regression.log`, `.runtime/program-lifecycle-uuid-race.log`,
  `.runtime/program_acl_probe.log`.
  Разбор первого общего CI — `.runtime/program-lifecycle-ci-failed.log`;
  последовательность миграционных/PG-тестов — `.runtime/program-lifecycle-order-fixed.log`.
  Успешный общий прогон — `.runtime/program-lifecycle-ci-success.log`.

Локальная приемка закончена: QA-вкладки закрыты, сервер на 8008 остановлен,
временный контейнер PostgreSQL 5448 удален вместе с синтетическими БД.
Пять исходных контейнеров пользователя продолжают работать; рабочая БД
не мигрировалась и не изменялась.

## Принятый результат: 61D-4

Код `103cf8b`; [CI 34292476098](https://github.com/Gordey-ap01/rm/actions/runs/34292476098)
прошел: `1114 passed, 490 warnings` за `841.77s`. Весь job — 22 минуты
30 секунд, backup/restore drill — 7 минут 14 секунд.

- Администратор/руководитель останавливает новые запуски из карточки серии.
  Возобновление доступно только руководителю после последнего явного STOP.
  Отмена занятий и снятие участия не считаются временной остановкой.
- Действие требует основания, UUID и ожидаемого последнего события.
  Повтор принятого UUID не пишет второе событие, даже после позднейших решений.
  Устаревшая форма получает 409; приоритет руководителя сохраняется.
- Сервис, модель и PostgreSQL guard `0066` проверяют один и тот же
  непосредственный STOP-предшественник. Миграционный preflight блокирует
  некорректную старую историю, ничего в ней не исправляя автоматически.
- Остановка прерывает незавершенные запуски. Возобновление разрешает новые
  запуски, но не создает занятия само по себе. Занятия, участия, назначения
  специалистов и финансовые факты сохраняются.
- Карточка, история и реестр различают остановленные и отмененные серии.
  Отмененные legacy-серии без журнала остаются в фильтре «Отменена».

Локальные подтверждения:

| Проверка | Результат |
| --- | --- |
| Новый модуль stop/resume | 13 тестов на PostgreSQL 17 без пропусков; 13 тестов на SQLite, один PostgreSQL-only пропуск |
| Существующие серии, реестр, редактор | 183 passed на PostgreSQL; реестр 10 passed на SQLite после исправления NULL-фильтра |
| Миграции | Чистая цепочка `0001-0066`; forward/reverse/preflight и raw SQL guard проверены |
| Browser QA | create/join stop/resume, stale POST 409, отказ resume после снятия участия, роли и фильтры |
| Сохранность фактов | После двух циклов stop/resume: исходные 4 занятия, 9 участий, 8 назначений и проводка `-1` |
| Мобильный браузер | Chrome, 390px: формы, карточка и реестр без переполнения; console warnings/errors отсутствуют |
| Независимое ревью | Блокирующих замечаний нет |
| Проверки, совпадающие с CI | Django check, makemigrations --check, Ruff и dependency manifests прошли |

Встроенный браузер не применил viewport override; мобильная приемка
выполнена в Chrome с измеренной шириной 390px. Это не проверка физического
телефона. Временные вкладки закрыты, browser-сервер остановлен,
отдельный PostgreSQL-контейнер удален. Рабочая БД не изменялась.

Расширенный запуск `ruff check .` дополнительно выявил 35 прежних замечаний
в `scripts/build_viewer.py` и `scripts/launcher.py`, неизменных с
`9624146`. Эти desktop packaging-скрипты вне нынешнего CI lint-scope;
это отдельный долг упаковки, а не исправленная часть 61D-4.

## Принятая основа

| Контур | Принятый результат / граница |
| --- | --- |
| Получатели, представители, расписание | Индивидуальные/групповые занятия, несколько специалистов, лимиты кабинетов, переносы и цепочки; еще нужна приемка плотного расписания центра |
| Финансы | Участник имеет собственный счет и решение по списанию; ledger, конкурентная запись, перенос остатков и money → sessions приняты; возвраты/обратная конвертация отдельно |
| Payroll | Ставки и утверждение руководителем, табель, передача в выплату, факт выплаты и неизменяемая история |
| Гранты, 58–60 | Редакции плана, fixed/per-session оплата, бюджет зарплаты, закрытые обезличенные снимки и приватная фактическая сдача; эксплуатационные условия 59B-2 еще открыты |
| Документы/сертификаты | Договоры, акты, согласия, шаблоны, импорт сертификатов, связь с балансом и preflight; production cleanup/правовая приемка еще нужны |
| Серии, 61A–61C-4d | create/join, история состава, единый materializer, retry/missing, сервисные stop/resume `0062`, cancel/withdraw с сохранением общего слота и чужих участий; новый UI stop/resume и более строгий guard `0066` относятся к 61D-4 выше |
| 61D-1 и контракт 62 | Рабочие retry/cancel/withdraw; ручное проведение и принятие расписания без ответа специалиста, реальный автор/основание, приоритет руководителя, `0065` |
| 61D-2 | Реестр по получателю, программе, статусу и периоду с исторической принадлежностью; CI 34229677232: 1086 passed |
| 61D-3 | Редактор будущего состава с preview, stale/replay-защитой и историей; CI 34236438524: 1101 passed, 486 warnings |
| Production-инструменты | Health, строгий preflight, backup/restore v2, recovery, разделение runtime/migration DB-ролей и CI restore-drill; рабочее production-развертывание не проводилось |

Последняя принятая до 61D-4 версия — `f06aefc`, контрольный документ
`259b3d6`. [CI 34236438524](https://github.com/Gordey-ap01/rm/actions/runs/34236438524)
прошел за 21 минуту 48 секунд: полный pytest `1101 passed, 486 warnings`
за `817.99s`; restore drill — 7 минут.
Подробная история сохранена без изменений в
[архиве по 61D-3](archive/recovery/current-state-through-2026-09-09-61d3.md).
Более ранний полный журнал — [архив по 2026-07-21](archive/recovery/current-state-through-2026-07-21.md).

## Что остается

1. До учебного выезда: выбрать ПК, пройти перезагрузку Windows/запуск без Codex
   по протоколу 65, подтвердить ответственного и людей. Затем провести
   приёмку сотрудниками. Жизненный цикл программы, переходы каскада и обзор
   руководителя уже реализованы и приняты в 61D-6/61D-7a–7c; заново их не строить.
   Редактор будущего состава и payroll также реализованы, но не входят
   в уже пройденные браузерные шаги целиком. Их рабочие сценарии принимаются
   отдельно с ответственными сотрудниками; отсутствие такой приёмки не равно
   отсутствию функции. Известное мелкое дублирование текста конфликта ещё открыто.
2. До реальных данных: выбрать рабочую площадку, настроить доступы, внешнюю
   защищённую копию и мониторинг, назначить ответственных и проверить
   восстановление этой площадки. Если в пилоте используется почта, нужны
   настройка SMTP, внешнего URL и отдельного процесса отправки. Программная
   очередь outbox/claim с повторами реализуется и проверяется в срезе 73.
   Для приватных грантовых документов дополнительно закрыть условия 59B-2:
   сроки хранения/сохранение при споре, проверку файлов и шифрование.
3. Перед миграциями рабочей БД к актуальной схеме по `0069`: backup v2,
   `--strict` preflight, временное закрытие грантовых записей, проверенная
   совместимая версия для отката приложения и production restore-drill.
4. До включения финансовых исключений определить процесс возвратов/обратной
   конвертации и исправления проведения после финансовых фактов. Отсутствующие
   безопасные операции реализуются отдельным согласованным срезом; защиту
   истории не обходить. Телефон/второй ПК проверяются на подготовленной сети,
   если включены в формат; текущий учебный тест рассчитан на один локальный ПК.
5. После стабилизации модели — общий Excel write-path. Постоянное изменение
   графика специалистов требует отдельного контракта. Не считать эти функции
   автоматически выполненными текущими разовыми согласованиями.

## Окружение и восстановление

Docker Desktop восстановлен после зависших socket-файлов. Только локальные
runtime-каталоги переименованы с сохранением копий; базы, тома и настройки
не сбрасывались. Остальные контейнеры пользователя сохранены и работают.
Не останавливать весь Docker ради очистки тестовой БД.

При обрыве: git status → текущий commit/run CI → верх этого файла →
профильный контракт. Локальные логи 61D-4 — `.runtime/stop-resume-*.log`;
они игнорируются Git. Принятый checkpoint и результат CI должны быть в Git,
чтобы продолжение не зависело от одной локальной сессии.

## Правила сохранности

- Один владелец `operations/models.py` и migration chain; остальные агенты
  работают в непересекающихся файлах. Финансы/расписание/полномочия закрываются
  целым сценарием: правило, сервис, UI, аудит, проверки, документация.
- Не запускать production backfill/import apply без preflight.
- `0048` не откатывается через `migrate 0047`. После новой истории
  нельзя откатывать `0053` (снимки), `0054` (сдачи/private),
  `0055/0056` (групповой состав/join), `0058/0059` (native revision/run/result),
  `0060` (cross-revision results), `0061` (retry targets),
  `0062` (stop/resume decisions), `0063` (cancel results), `0064` (withdraw/lineage),
  `0065` (операторские решения), `0067` (решения по программе).
  Откат приложения сохраняет аддитивную
  схему, immutable-историю и private volume.
- Reverse `0066` проверен для миграционного round-trip, но ослабляет guard;
  production rollback не должен обходить новое доменное ограничение.
- Не включать SMTP как надежную доставку до transactional outbox/claim.
- Не объединять runtime/migration DB-роли и не давать runtime DDL,
  отключение triggers или замену защитных функций.
- Не хранить ключи, пароли, production-конфиги и реальные ПДн в Git.
- SQLite не доказывает PostgreSQL concurrency. Не повторять принятые
  проверки без новых изменений, failures или конкретного нерешенного риска.
- Миграционные тесты восстанавливают текущие `leaf_nodes` после своих assertions.
  После изменения схемы проверять pytest-последовательность соседних классов:
  отдельный `manage.py test` может не обнаружить утечку схемы между модулями.
- Не возвращаться к очередным микросрезам документов/сертификатов без
  продуктового приоритета PRD.

Источники требований: [PRD](01-prd.md), [доменная модель](07-updated-domain-model-after-interview.md),
[полномочия](decisions/ADR-003-approval-authority-and-manual-resolution.md),
[контракт серий](61-group-program-series-lifecycle-contract.md),
[ручные решения](62-operator-attendance-and-schedule-decisions.md).
Полный индекс — [манифест восстановления](project-recovery-manifest.md).

# Текущее состояние проекта

Дата продолжения: 2026-09-13. Ветка: `codex/domain-appointments-v2`.

## Откуда продолжать

61D-6 принят локально и в общем CI: активация/завершение/отмена программы с
рассмотренным составом, явными ролями и SQL guards. Код `67d61cd`;
[CI 34693080115](https://github.com/Gordey-ap01/rm/actions/runs/34693080115)
успешен на `67d61cdf0b140bde1799bb37de5869852dabe1e4`: **1154 passed,
498 warnings за 683.91s**. Job приложения — 12m30s, параллельное
восстановление резервной копии — 7m26s. Полный лог сохранен в
`.runtime/program-completion-ci-success.log`.

61D-7a локально принят: карточки программы/получателя показывают
план, назначения, фактически проведенные участия и остаток. Списания
сохраняются отдельно. Незакрытый каскад отображается по фактам как
«Запланирован», «Расписан» или «Идёт»; поздние факты не меняют явные
терминальные статусы. Мастер и три materializer-path используют условный
переход planned -> scheduled внутри транзакции назначения, без регресса
состояния или частичного сохранения. Общий локальный PostgreSQL gate
нового helper, проекции и lifecycle UI: **33 passed за 68.55s**, native exit 0;
лог `.runtime/program-progress-final.log`. Код сохранен в `5f538de`;
[общий CI 34696564220](https://github.com/Gordey-ap01/rm/actions/runs/34696564220)
на `5f538de82247afbda569c341cbc2700dc5479642` завершился: 1165 passed и
1 failed за 875.65s, 500 warnings. Единственный failure — UI-тест ожидал
старый текст пояснения после финальной правки шаблона. Ожидание исправлено;
тот же тест прошел отдельно (1 passed за 35.26s, native exit 0). Причина —
текст был изменен после локального gate; итоговая редакция должна проверяться
после последнего изменения. Повторный общий CI еще требуется. Лог первого
CI — `.runtime/program-progress-ci-failed.log`, исправления —
`.runtime/program-progress-copy-fix.log`. Restore job прошел.
Browser QA на синтетических данных: при фактических 390px показатели
каскада отображаются карточкой без горизонтальной прокрутки; при 1280px —
таблицей. Пример plan=4, allocated=2, completed=1, remaining=3, charged=2
совпадает в карточках программы и получателя. Обе страницы без page overflow,
console errors/warnings отсутствуют. Проверка выполнена на актуальных
исходных стилях (отдельный dev-server 8012 с DJANGO_DEBUG=1; старый QA 8011
отдавал ранее собранный staticfiles). Viewport восстановлен, вкладка закрыта.

Схема `0001-0068` развернулась в одноразовой PostgreSQL;
Django/Ruff/manifests/drift checks прошли. Независимое ревью выявило
необходимость проверки canonical fingerprint в SQL и инверсию порядка
блокировок в Django admin с inline-каскадами. Обе исправлены; реальная
конкурентная проверка admin POST подтверждает ожидание блока до захвата корня.

Новый модуль содержит 19 проверок. Последовательный PostgreSQL gate старых
миграционных проверок, нового модуля и измененной composition-фикстуры:
23 passed за 101.49s. UI-модуль: 21 passed за 32.60s. Отдельный старый
PostgreSQL concurrency-класс серий: 22 passed за 95.17s, native exit 0.
Первоначальный полный локальный файл серий оборвался без итогового отчета
после 116 отметок; причина завершения не установлена, успешным полным прогоном
он не считается. Повтор выполнен для конкурентного класса с faulthandler и
сохранением native exit; весь файл затем прошел в общем CI `34693080115`.

Ограниченная runtime-role выполнила activate/pause/resume/cancel, а UPDATE/DELETE
истории были запрещены. Browser QA: активация на 390px, досрочное завершение
руководителем, отмена, stale 409 и обновление проверки, terminal block form
без 500; названия услуг вместо внутренних ID, console чистая. Временные
QA-серверы 8008/8009 остановлены владельцем. Все данные синтетические.

Отдельно внесен `921b3a7`: CI разделен на независимые тесты приложения и
backup/restore drill. [Первый run 34692002951](https://github.com/Gordey-ap01/rm/actions/runs/34692002951)
успешен на `921b3a7`: 1132 passed, 495 warnings за 930.62s; job приложения
16m41s, отдельный restore job 7m30s. Общее ожидание около 17 минут. Это
измерение одного запуска; суммарная стоимость runner-минут не обязана снижаться.
Логи: `.runtime/ci-parallel-accepted.log`, `.runtime/program-completion-migrate.log`,
`.runtime/program-completion-series-regression.log`, `.runtime/program-completion-series-pg68.log`.

Следующий 61D-7b — явное завершение/отмена каскада с рассмотренными фактами;
затем руководительский обзор и общая ролевая приемка. Решение должно получать
согласованный снимок одним запросом, а не использовать трехзапросный helper
обзора. Новые назначения и поздний учет существующего занятия — разные
сценарии: блокировка новых назначений не должна запрещать фактическую отметку.
Принятие пакета — контрольная точка, а не автоматическая остановка продолжения.

Docker 12 сентября запущен пользователем; API отвечает. Причина отсутствия
автозапуска — `AutoStart: false` в настройках Docker Desktop и отключенная
запись Docker Desktop в Windows StartupApproved (03). Настройки не менялись.
Рабочие БД не мигрировались. Временный контейнер для приемки
61D-6/61D-7a: `rm-program-lifecycle-qa`, порт 5448, только синтетические данные.
После локальной приемки QA-серверы остановлены, временный контейнер удален.
Пять контейнеров пользователя продолжают работать.

Предыдущий полностью принятый функциональный пакет (историческая точка):

61D-5 принят локально и в общем CI: пауза/возобновление программы, журнал решений,
защита ручного назначения и новых определений групповых серий. Модель,
сервис, UI и миграция `0067` проверены.
Актуальный commit после исправления изоляции тестов —
`07b632f83a4281d3ab6a51a6854743258b041082`.
[Повторный CI 34317851480](https://github.com/Gordey-ap01/rm/actions/runs/34317851480)
завершился успешно на этом commit: `1132 passed, 495 warnings` за `725.86s`.
Весь job — 20 минут 31 секунда; backup/restore drill — 7 минут 6 секунд.
Функциональный код внесен в `dde62b6`; `07b632f` исправляет изоляцию тестов.
После обрыва сначала проверить текущий commit и результат CI;
не повторять уже пройденные локальные проверки без новой ошибки/изменения.

Активный этап — завершение рабочих сценариев программ, каскадов и серий
по контракту 61. Принятый до этого срез 61D-4 сохранен ниже;
полную готовность проекта к рабочему пилоту эти срезы не означают.

Границы принятых переходов закреплены в разделах 61D-5/61D-6
контракта 61. После них остаются явные переходы каскада, руководительский
обзор и общая ролевая приемка. Нельзя
превращать обычное редактирование статуса в массовую отмену расписания
либо финансовую корректировку.

Объяснение для владельца центра и порядок доведения до пилота:
[карта продолжения проекта](project-roadmap-2026-09-09.md).
Она является планом, а не заменой требований PRD.

## Принятый результат: 61D-5

- Пауза/возобновление программы имеют отдельные команды и неизменяемый журнал.
  Пауза доступна администратору/руководителю; возобновление — руководителю.
  Принятый UUID распознается до stale/status-проверок; история руководителя
  имеет приоритет. Старые paused-программы не получают вымышленный backfill.
- Пауза не переписывает расписание и финансовые факты. Новые назначения
  проверяют программу под блокировкой; обычные заметки и учет проведенного
  занятия остаются доступны. Возобновление само не создает занятия.
- Исправлена конкурентная перезапись привязки участника старой формой:
  сохранение считывает актуального участника под блокировкой, проверяет
  текущий и выбранный каскады; при отсутствии изменений запись не выполняется.
- Новые блокировки выявили race повторного UUID при создании/join серии:
  повтор мог завершиться ValidationError на full_clean до INSERT. Теперь
  обработчик проверяет уже принятый UUID и его исходные данные; при отсутствии
  такого ключа исходная ошибка остается ошибкой. Оба прежних PG-теста
  одновременного создания прошли после исправления; повторное ревью прошло.
- Чистая PostgreSQL-цепочка `0001-0067` прошла. Restricted runtime-role
  выполнила обе команды, а UPDATE/DELETE нового журнала были запрещены.
- Независимое ревью после исправления замечания прошло. Browser QA:
  admin pause, director resume, приоритет, stale 409, replay без нового события,
  история; Chrome 390px без переполнения и console errors. Обратная ссылка
  «К получателю» проверена; новый переход из карточки получателя отдельно
  в браузере не проверялся. Все данные синтетические.
- Новый модуль: PostgreSQL 18 passed за 22.546s; SQLite 15 passed и 3
  PostgreSQL-only skips. Включены raw SQL/deferred guards, migration round-trip,
  preflight и запрет reverse после истории; гонка подтверждает ожидание
  писателя через `pg_blocking_pids`, затем отказ назначения после паузы.
- Старые серии/реестр/состав/stop-resume: 192 passed, 4 failed в первом прогоне
  за 432.40s. Две проверки UUID исправлены в коде, две старые проверки
  адаптированы к новому запрету пустой серии и перечитыванию состояния.
  Все четыре прошли отдельно: UUID 2 passed за 36.42s; остальные 2 passed
  за 2.114s. Ruff, Django check, migration dry-run, manifests и diff-check чистые.
- Первый [общий CI 34315830339](https://github.com/Gordey-ap01/rm/actions/runs/34315830339)
  дал 1129 passed и 3 failed: старый класс операторских миграционных тестов
  оставлял схему на `0065` перед новыми PG-проверками. Добавлен tearDown,
  восстанавливающий текущие leaf nodes после assertions. Последовательный
  pytest старого класса и новых PG-тестов прошел: 11 passed за 27.33s.
  Повторный общий CI выше закрыл все 1132 теста; guards приложения не ослаблялись.
- Логи этого среза:
  `.runtime/program-lifecycle-regression.log`, `.runtime/program-lifecycle-uuid-race.log`,
  `.runtime/program_acl_probe.log`.
  Разбор первого общего CI — `.runtime/program-lifecycle-ci-failed.log`;
  последовательность миграционных/PG-тестов — `.runtime/program-lifecycle-order-fixed.log`.
  Успешный общий прогон — `.runtime/program-lifecycle-ci-success.log`.

Локальная приемка закончена: QA-вкладки закрыты, сервер на 8008 остановлен,
временный контейнер PostgreSQL 5448 удален вместе с синтетическими БД.
Пять исходных контейнеров пользователя продолжают работать; рабочая БД
не мигрировалась и не изменялась.

## Принятый результат: 61D-4

Код `103cf8b`; [CI 34292476098](https://github.com/Gordey-ap01/rm/actions/runs/34292476098)
прошел: `1114 passed, 490 warnings` за `841.77s`. Весь job — 22 минуты
30 секунд, backup/restore drill — 7 минут 14 секунд.

- Администратор/руководитель останавливает новые запуски из карточки серии.
  Возобновление доступно только руководителю после последнего явного STOP.
  Отмена занятий и снятие участия не считаются временной остановкой.
- Действие требует основания, UUID и ожидаемого последнего события.
  Повтор принятого UUID не пишет второе событие, даже после позднейших решений.
  Устаревшая форма получает 409; приоритет руководителя сохраняется.
- Сервис, модель и PostgreSQL guard `0066` проверяют один и тот же
  непосредственный STOP-предшественник. Миграционный preflight блокирует
  некорректную старую историю, ничего в ней не исправляя автоматически.
- Остановка прерывает незавершенные запуски. Возобновление разрешает новые
  запуски, но не создает занятия само по себе. Занятия, участия, назначения
  специалистов и финансовые факты сохраняются.
- Карточка, история и реестр различают остановленные и отмененные серии.
  Отмененные legacy-серии без журнала остаются в фильтре «Отменена».

Локальные подтверждения:

| Проверка | Результат |
| --- | --- |
| Новый модуль stop/resume | 13 тестов на PostgreSQL 17 без пропусков; 13 тестов на SQLite, один PostgreSQL-only пропуск |
| Существующие серии, реестр, редактор | 183 passed на PostgreSQL; реестр 10 passed на SQLite после исправления NULL-фильтра |
| Миграции | Чистая цепочка `0001-0066`; forward/reverse/preflight и raw SQL guard проверены |
| Browser QA | create/join stop/resume, stale POST 409, отказ resume после снятия участия, роли и фильтры |
| Сохранность фактов | После двух циклов stop/resume: исходные 4 занятия, 9 участий, 8 назначений и проводка `-1` |
| Мобильный браузер | Chrome, 390px: формы, карточка и реестр без переполнения; console warnings/errors отсутствуют |
| Независимое ревью | Блокирующих замечаний нет |
| Проверки, совпадающие с CI | Django check, makemigrations --check, Ruff и dependency manifests прошли |

Встроенный браузер не применил viewport override; мобильная приемка
выполнена в Chrome с измеренной шириной 390px. Это не проверка физического
телефона. Временные вкладки закрыты, browser-сервер остановлен,
отдельный PostgreSQL-контейнер удален. Рабочая БД не изменялась.

Расширенный запуск `ruff check .` дополнительно выявил 35 прежних замечаний
в `scripts/build_viewer.py` и `scripts/launcher.py`, неизменных с
`9624146`. Эти desktop packaging-скрипты вне нынешнего CI lint-scope;
это отдельный долг упаковки, а не исправленная часть 61D-4.

## Принятая основа

| Контур | Принятый результат / граница |
| --- | --- |
| Получатели, представители, расписание | Индивидуальные/групповые занятия, несколько специалистов, лимиты кабинетов, переносы и цепочки; еще нужна приемка плотного расписания центра |
| Финансы | Участник имеет собственный счет и решение по списанию; ledger, конкурентная запись, перенос остатков и money → sessions приняты; возвраты/обратная конвертация отдельно |
| Payroll | Ставки и утверждение руководителем, табель, передача в выплату, факт выплаты и неизменяемая история |
| Гранты, 58–60 | Редакции плана, fixed/per-session оплата, бюджет зарплаты, закрытые обезличенные снимки и приватная фактическая сдача; эксплуатационные условия 59B-2 еще открыты |
| Документы/сертификаты | Договоры, акты, согласия, шаблоны, импорт сертификатов, связь с балансом и preflight; production cleanup/правовая приемка еще нужны |
| Серии, 61A–61C-4d | create/join, история состава, единый materializer, retry/missing, сервисные stop/resume `0062`, cancel/withdraw с сохранением общего слота и чужих участий; новый UI stop/resume и более строгий guard `0066` относятся к 61D-4 выше |
| 61D-1 и контракт 62 | Рабочие retry/cancel/withdraw; ручное проведение и принятие расписания без ответа специалиста, реальный автор/основание, приоритет руководителя, `0065` |
| 61D-2 | Реестр по получателю, программе, статусу и периоду с исторической принадлежностью; CI 34229677232: 1086 passed |
| 61D-3 | Редактор будущего состава с preview, stale/replay-защитой и историей; CI 34236438524: 1101 passed, 486 warnings |
| Production-инструменты | Health, строгий preflight, backup/restore v2, recovery, разделение runtime/migration DB-ролей и CI restore-drill; рабочее production-развертывание не проводилось |

Последняя принятая до 61D-4 версия — `f06aefc`, контрольный документ
`259b3d6`. [CI 34236438524](https://github.com/Gordey-ap01/rm/actions/runs/34236438524)
прошел за 21 минуту 48 секунд: полный pytest `1101 passed, 486 warnings`
за `817.99s`; restore drill — 7 минут.
Подробная история сохранена без изменений в
[архиве по 61D-3](archive/recovery/current-state-through-2026-09-09-61d3.md).
Более ранний полный журнал — [архив по 2026-07-21](archive/recovery/current-state-through-2026-07-21.md).

## Что остается

1. Оставшийся 61D: активация, завершение/отмена программы, переходы каскада, руководительский обзор
   и общая ролевая приемка. Требуемое поведение уже описано в контракте 61,
   детализация следующего ограниченного среза еще не является реализацией.
2. До использования реальных данных закрыть production-допуск 59B-2:
   transactional outbox перед SMTP, внешняя резервная копия, мониторинг,
   шифрование, сроки хранения/сохранение при споре, проверка загружаемых файлов,
   раздельные учетные данные БД вне Git.
3. Перед миграциями рабочей БД `0048-0067`: backup v2, `--strict` preflight,
   временное закрытие грантовых записей, проверенная совместимая версия для
   отката приложения и production restore-drill.
4. До рабочего пилота определить процесс возвратов/обратной конвертации и
   исправления проведения после финансовых фактов, провести обезличенные
   сценарии центра и проверку мобильного кабинета на физическом телефоне.
5. После стабилизации модели — общий Excel write-path. Постоянное изменение
   графика специалистов требует отдельного контракта. Не считать эти функции
   автоматически выполненными текущими разовыми согласованиями.

## Окружение и восстановление

Docker Desktop восстановлен после зависших socket-файлов. Только локальные
runtime-каталоги переименованы с сохранением копий; базы, тома и настройки
не сбрасывались. Остальные контейнеры пользователя сохранены и работают.
Не останавливать весь Docker ради очистки тестовой БД.

При обрыве: git status → текущий commit/run CI → верх этого файла →
профильный контракт. Локальные логи 61D-4 — `.runtime/stop-resume-*.log`;
они игнорируются Git. Принятый checkpoint и результат CI должны быть в Git,
чтобы продолжение не зависело от одной локальной сессии.

## Правила сохранности

- Один владелец `operations/models.py` и migration chain; остальные агенты
  работают в непересекающихся файлах. Финансы/расписание/полномочия закрываются
  целым сценарием: правило, сервис, UI, аудит, проверки, документация.
- Не запускать production backfill/import apply без preflight.
- `0048` не откатывается через `migrate 0047`. После новой истории
  нельзя откатывать `0053` (снимки), `0054` (сдачи/private),
  `0055/0056` (групповой состав/join), `0058/0059` (native revision/run/result),
  `0060` (cross-revision results), `0061` (retry targets),
  `0062` (stop/resume decisions), `0063` (cancel results), `0064` (withdraw/lineage),
  `0065` (операторские решения), `0067` (решения по программе).
  Откат приложения сохраняет аддитивную
  схему, immutable-историю и private volume.
- Reverse `0066` проверен для миграционного round-trip, но ослабляет guard;
  production rollback не должен обходить новое доменное ограничение.
- Не включать SMTP как надежную доставку до transactional outbox/claim.
- Не объединять runtime/migration DB-роли и не давать runtime DDL,
  отключение triggers или замену защитных функций.
- Не хранить ключи, пароли, production-конфиги и реальные ПДн в Git.
- SQLite не доказывает PostgreSQL concurrency. Не повторять принятые
  проверки без новых изменений, failures или конкретного нерешенного риска.
- Миграционные тесты восстанавливают текущие `leaf_nodes` после своих assertions.
  После изменения схемы проверять pytest-последовательность соседних классов:
  отдельный `manage.py test` может не обнаружить утечку схемы между модулями.
- Не возвращаться к очередным микросрезам документов/сертификатов без
  продуктового приоритета PRD.

Источники требований: [PRD](01-prd.md), [доменная модель](07-updated-domain-model-after-interview.md),
[полномочия](decisions/ADR-003-approval-authority-and-manual-resolution.md),
[контракт серий](61-group-program-series-lifecycle-contract.md),
[ручные решения](62-operator-attendance-and-schedule-decisions.md).
Полный индекс — [манифест восстановления](project-recovery-manifest.md).

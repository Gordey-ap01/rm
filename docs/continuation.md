# Короткая точка продолжения

Обновлено 14 сентября 2026 года. Репозиторий `RMcodex`, ветка
`codex/domain-appointments-v2`. Подробные доказательства: [current-state](current-state.md).

## Главное требование владельца

Администратор может отметить проведение и принять расписание без ответа
специалиста. Система сохраняет фактического автора, роль и основание.
Руководитель имеет приоритет; нижестоящее решение не заменяет его итог.
Согласие получателя, учет проведения и финансовые операции остаются отдельными
действиями. Исправление после списаний/начислений требует отдельного процесса.

## Готовый учебный комплект

Финальный комплект: `dist/RM-pilot-5cae343ee720`, собранный из
`5cae343ee720b580f4a09ca4d1d65aa103d12463`. Запуск и остановка на Windows:
`START_PILOT.bat` / `STOP_PILOT.bat`; URL — `http://127.0.0.1:18000`.
PostgreSQL наружу не публикуется. Seed учебного дня установлен на 21 сентября
2026 года: четыре учетные записи в трех ролях (администратор, руководитель,
два специалиста). Общий пароль задан только в
`pilot/.pilot-local.env` переменной `RM_PILOT_PASSWORD`; значение никогда не
заносить в Git, документы или логи.

Локальная приемка 64 завершена: Start/Stop/Start на Windows PowerShell 5.1,
hash guard, изоляция Docker, seed без дублей, UI-persistence заметки, director
и specialist роли, loopback/DB safety приняты. Полные доказательства и логи —
в [current-state](current-state.md) и [контракте 64](64-local-pilot-kit-contract.md).
`c45c97d` создал комплект, `1c734b5` исправил Windows export, `bfd3ea6` —
полномочия руководителя, `5cae343` — закрытые асинхронные фильтры. Проект
оставлен запущенным; старые `1c`/`bfd` остановлены с сохраненными volumes.

Общий [CI 34778462107](https://github.com/Gordey-ap01/rm/actions/runs/34778462107)
точного `5cae343` успешен: 1243 passed, 522 warnings, 1045.22s; backup/restore
успешен. Копия `dist/pilot-backups/5cae343ee720-pretest-20260914.dump` проверена
восстановлением в отдельную PostgreSQL: записи и заметка совпали. Это снимок БД
после технической проверки, до теста сотрудников; файлы и настройки в него не входят.

## Откуда продолжать

Отложенная идея владельца: [отдельный информационный экран центра](ideas/center-information-display.md)
с обязательными часами и текущей занятостью кабинетов. **Пока не реализовывать.**
При содержательном продолжении проекта коротко напоминать о ней в итоговом
статусе, не чаще одного раза за сессию. Напоминание не меняет очередность
подготовки выездного комплекта и не считается разрешением начать разработку.

1. Техническая приемка комплекта завершена. Не повторять принятые проверки без
   изменения кода или нового риска; не пересобирать комплект из коммита документации.
2. Провести отдельную репетицию на компьютере выезда, включая перезапуск
   Windows, только после решения владельца. Комплект уже содержит учебную
   PostgreSQL, отдельные роли, синтетические примеры, loopback URL и изолированное
   хранение.
3. Первый тест с людьми — ориентир 21 сентября 10:00–12:30, подтверждение
   18 сентября до 16:00. Реальный пилот — ориентир 5 октября, подтверждение
   2 октября до 16:00, резерв 12 октября при принятом допуске. Даты и роли
   пока не подтверждены сотрудниками. Подробный план и текст уведомления:
   [63-first-field-test-and-pilot-plan.md](63-first-field-test-and-pilot-plan.md).
4. Для реального пилота нужны эксплуатационные решения: ответственное лицо,
   обезличенные примеры, реальная площадка, внешние копии, почта/мониторинг и
   порядок финансовых исключений. Общий Excel-импорт и постоянный график
   не добавлять в этот этап без отдельного определения объема.

## Восстановление после закрытия приложения

Причина закрытия Codex не установлена и не исправлена: Crashpad содержит только
метаданные, без дампа. После закрытия приложения контейнеры предшественника
оставались healthy около часа; это не доказывает причинную связь. Пять контейнеров
пользователя и рабочие БД не затрагивались.

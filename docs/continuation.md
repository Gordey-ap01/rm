# Короткая точка продолжения

Обновлено 13 сентября 2026 года. Репозиторий `RMcodex`, ветка
`codex/domain-appointments-v2`. Подробные доказательства: [current-state](current-state.md).

## Главное требование владельца

Администратор может отметить проведение и принять расписание без ответа
специалиста. Система сохраняет фактического автора, роль и основание.
Руководитель имеет приоритет; нижестоящее решение не заменяет его итог.
Согласие получателя, учет проведения и финансовые операции остаются отдельными
действиями. Исправление после списаний/начислений требует отдельного процесса.

## Что уже сохранено

- `a497013` — завершение/отмена каскада, рассмотренные факты, история,
  полномочия и миграция `0069`. Администратор завершает выполненный план,
  руководитель может завершить досрочно с основанием. Новые назначения после
  закрытия запрещены, поздняя отметка существующего занятия разрешена.
  [CI 34718948780](https://github.com/Gordey-ap01/rm/actions/runs/34718948780):
  1195 passed, восстановление резервной копии прошло.
- `34697fb` — обзор программ и каскадов в существующих панели и очереди:
  четыре фильтра, страницы по 20 объектов, фактический прогресс, переходы к
  защищенным командам. Локальные PostgreSQL-проверки и браузер 390px/1280px
  приняты. Исправлены сброс фильтра и включение задач в «Следующее действие».
  [CI 34754752273](https://github.com/Gordey-ap01/rm/actions/runs/34754752273)
  прошел на `34697fbdce1fe7ab6d02ad8723662a687e45b1ba`: 1207 passed,
  513 warnings за 18m13s. Восстановление резервной копии также прошло.

## Откуда продолжать

1. Сверить текущий commit и `git status`. Функциональный head общего CI указан
   выше; следующий checkpoint меняет только документацию. Не повторять
   успешно принятые проверки без изменения кода или нового обнаруженного риска.
2. Пройти общий сценарий рабочего дня на синтетических данных: групповое
   занятие, решение администратора без специалиста, приоритет руководителя,
   фактический прогресс, закрытие каскада, сохранение финансовой истории.
   Отдельные части покрыты контрактами 53, 55, 61 и 62; новая общая приемка
   должна соединить их, а не создавать новые доменные правила.
3. Затем пилот центра и эксплуатационные решения: ответственное лицо,
   обезличенные примеры, реальная площадка, внешние копии, почта/мониторинг и
   порядок финансовых исключений. Общий Excel-импорт и постоянный график
   не добавлять в этот этап без отдельного определения объема.

## Восстановление после закрытия приложения

Сбой окна Codex не равнозначен лимиту модели, отказу Docker или ошибке проекта.
Не объявлять причину без журналов. Локальная диагностика 13 сентября нашла
метаданные Crashpad за 12 сентября 22:36 и 13 сентября 07:36; дампа с причиной
нет. Краткий диагностический отчет сохраняется локально в `.runtime` и не
отправляется автоматически.

Временный QA-сервер 8013 завершен; одноразовый контейнер
`rm-block-lifecycle-qa` удален после приемки. Пять контейнеров пользователя
сохранены. Рабочие БД не мигрировались. Docker Desktop не настроен на
автозапуск: это отдельный подтвержденный факт, не установленная причина сбоя Codex.

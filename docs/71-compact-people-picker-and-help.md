# 71: компактный поиск людей и пояснения по запросу

15 сентября 2026 года владелец уточнил: пояснения нужны под знаком «?» возле
полей, а полный список получателей/специалистов не подходит для большого центра.

Код `af36d612db766969481ce9eea183038fd79bb2c6`, установленный комплект
`dist/RM-pilot-af36d612db76`, URL `http://127.0.0.1:18000`.

## Изменения

- Пояснения скрыты под «?»: наведение, фокус клавиатуры и нажатие открывают
  текст, Escape закрывает. Подсказки работают и на сенсорном экране.
- Получатели и специалисты: отдельные выбранные имена с кнопкой удаления,
  счётчик и поиск по имени/фамилии. В исходном HTML только выбранные люди.
- Серверный GET `/appointments/people-search/` возвращает по 10 совпадений;
  «Назад / Далее» заменяют страницу, новые строки не удлиняют форму.
  Поиск разбит по словам; q ограничен 100 символами, номер страницы — 1–1000.
  Специалисты только активные, получатели соответствуют допускам формы.
- Выбранные значения остаются при другом поиске и сбое сети. Есть состояния
  загрузки, отсутствия совпадений и повторной попытки. Запоздавший ответ не
  заменяет новый поиск. Клавиатура: стрелки, Enter для выбора, Escape.
- Полный queryset серверной проверки сохранён: ограничены только варианты
  виджета. Маркер `lists`, защита финансовой истории и прежний POST совместимы.
  Доступ к поиску ограничен теми же операторами, что создание/редактирование;
  ответы `private, no-store`. Схема БД не менялась.

## Проверка

**19 passed, 1 warning, 47.69s**: поиск/пагинация/права, неактивный специалист,
начальный и повторный выбор вне первой страницы, недопустимый ID, создание,
конфликт, удаление участника и защита истории. Включена исправленная проверка
prefill из старого `operations/tests.py`. Лог `.runtime/compact-people-tests.log`.
Ruff всей области CI, dependency manifests и независимое ревью пройдены.

В отдельной QA-копии добавлены 1000 получателей и 250 специалистов. Chromium
320/768/1024/1440 px: компактные поля, поиск дальних записей, смена страниц,
выбор клавиатурой, снятие выбора, пустой ответ, 503 и повтор, подсказки и Escape.
Проверено нажатие на подсказку в touch-режиме. Создано отдельное QA-занятие
с получателем №0999 и специалистом №0249, подтверждён переход в его карточку.
Ошибок JavaScript нет. Доказательства `.runtime/compact-people-browser.json`,
`.runtime/compact-people-browser.log`, `.runtime/compact-people-{form,search}-*.png`.
Эти 1250 людей и QA-занятие в пользовательскую БД не переносились.

Полный [CI 34877091703](https://github.com/Gordey-ap01/rm/actions/runs/34877091703)
точного `af36d61` запущен и пока ожидается; агент сохраняет результат
в `.runtime/compact-people-ci.log` без повторного запуска.
Предыдущий CI `6349977`, run `34866385869`: 1260 passed, 1 failed,
529 warnings, 769.45s. Единственное падение ожидало старый `<option selected>`
в prefill-тесте. Проверка обновлена на выбранного участника/checkbox и прошла
в текущих 19 тестах. Backup/restore, миграции, check, Ruff/manifests были успешны.

## Сохранность и другой ПК

После остановки web `6349977` его БД восстановлена в проверенно пустую БД
нового комплекта: хеши содержимого и числа строк всех **102 таблиц совпали**.
Сохранены занятия №1/№2, больничный, решения и текущий вход `pilot-admin`.
Файлов media/private-artifacts не было; seed сохранил уже существующие данные.
Новые web/db healthy. Прежний комплект остановлен, volumes сохранены;
временные QA-сервер/контейнер удалены. Пять пользовательских контейнеров не трогали.

БД-копия: `dist/pilot-backups/6349977c08d9-before-compact-people-20260915.dump`,
905162 bytes, SHA-256
`0c48157de2aab262a117362c94ca01dd16de72b3a03e415a2cd7bfe15021e77f`.
Это только БД, без файлов/env; сравнение в
`.runtime/compact-people-preservation-20260915.json`.

Другой ПК: `dist/RM-pilot-af36d612db76-other-pc.zip`, 283 файла, 1442002 bytes,
SHA-256 `259ea5e3735d6465ce57cb5fa07eadc65c9303fbcf9a80a879fe97f25a566deb`.
Manifest/хеши/чтение ZIP проверены, БД/env/секреты/Docker-образы не включены.
Фактический запуск на другом ПК, его перезагрузка и внешний хостинг ещё впереди.

## Точка совместного продолжения

В первой пользовательской вкладке `/appointments/2/edit/` обнаружены набранные
изменения: два получателя/два специалиста, группа, «Согласовано», программа/счёт.
Агент не сохранял, не перезагружал и не закрывал эту вкладку.
**Не терять этот черновик при продолжении.**

Обновление показано в отдельной вкладке №2 (`compactFormTab`): сохранённое занятие
№2, 17 сентября 10:00–10:45, индивидуальное, один получатель/специалист,
«Предложено». Подтверждено чтением UI и скриншотом. Новый дизайн ожидает оценки
владельца; принятие бизнес-изменений выполняет он сам. Продолжать по одному
заданию из [журнала 67](67-guided-role-acceptance.md).
